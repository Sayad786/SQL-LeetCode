-- LeetCode 1050: Actors and Directors Who Cooperated At Least Three Times
-- Return actor_id and director_id pairs that appear at least 3 times

SELECT
    actor_id,
    director_id
FROM ActorDirector
GROUP BY actor_id, director_id
HAVING COUNT(*) >= 3;

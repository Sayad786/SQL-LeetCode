-- LeetCode 620: Not Boring Movies
-- Return movies with odd id and description not equal to 'boring'
-- Order by rating in descending order

SELECT
    id,
    movie,
    description,
    rating
FROM Cinema
WHERE MOD(id, 2) = 1
  AND description != 'boring'
ORDER BY rating DESC;
